# PDFs go here

Upload PDF reports into this folder, then run the GitHub Action:

**Actions → PDF to Xiaohongshu notes → Run workflow**

Default workflow input:

```text
input_dir: pdfs
output_dir: xhs_notes
```

Only files ending in `.pdf` will be processed. This README file is just a visible placeholder and will be ignored by the workflow.
